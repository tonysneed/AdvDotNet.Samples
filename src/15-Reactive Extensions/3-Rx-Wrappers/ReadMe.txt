Reactive Extensions Wrappers ReadMe

This solution consists of a web app with a basic WCF service that returns an array
of prime numbers based on the number of characters passed into a method.  There is
also a client application which calls the service operation.  This is where we will
be placing the Rx code.

